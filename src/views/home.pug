doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Joom
    body
        header Joom 
        main
            div#nick
                form
                    h3 Nickname
                    input(placeholder = 'Input nick name', required, type = "text")
                    button Save
            div#room
                form
                    h3 Create / Join Room
                    input(placeholder = 'Input room name', required, type = "text")
                    button Join / Create Room

        script(src="../socket.io/socket.io.js")
        script(src="../public/js/app.js")